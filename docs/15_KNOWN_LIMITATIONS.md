# 15. Known Limitations

- **Complex Tables**: Nested tables inside PDF RFPs might fail to parse cleanly in early versions.
- **Latency**: Generating drafts with multiple verification passes may take up to a minute depending on network/API loads.
